import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    skillListContainer: {
        height: 'auto',
        display: 'flex',
        flexBasis: 'content',
        flexDirection: 'column'
    }
})